- defaults:
    link:
      create: true
      relink: true
      force: true
    clean:
      force: true

- clean: ['~', '~/.config']

- shell:
  - [git submodule update --init --recursive, Installing/updating submodules]
